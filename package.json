{
  "name": "static-landing-page",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "export": "nuxt export",
    "serve": "nuxt build && nuxt export && nuxt serve"
  },
  "dependencies": {
    "@nuxtjs/device": "^1.2.7",
    "nuxt": "^2.13.0"
  },
  "devDependencies": {
    "@nuxtjs/style-resources": "^1.0.0",
    "node-sass": "^4.14.1",
    "sass-loader": "^9.0.2"
  }
}
